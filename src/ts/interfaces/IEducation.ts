export interface IStageEducation {
  id: number;
  name: string;
}

export interface IProfile {
  id: number;
  name: string;
}

export interface ISpecialization {
  id: number;
  name: string;
  code: string;
}
